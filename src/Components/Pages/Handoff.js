import React, { Component } from "react";

  class Handoff extends Component {
    render() {
      return (
        <div>
          <h1> Hand off</h1>
        </div>

      );
    }
  }

export default Handoff;